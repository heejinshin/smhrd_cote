# 제출한 코드 - "AB"만 통과, 소문자 섞인 문자열 인풋시 오류남 
# 사용 함수
  #ord -> 문자의 위치값 출력
  #chr -> ord의 반대로서, 해당 위치값에 해당하는 문자 출력
def solution(s, n):
    len_s = len(s)
    r = ord(s[0])+n 
    result = ""
    for i in range(len_s):
        result += chr(r+i)
    return result

# 입력값 〉	"a B z", 4
# 기댓값 〉	"e F d"
# 실행 결과 〉	실행한 결괏값 "efghi"이 기댓값 "e F d"과 다릅니다.
