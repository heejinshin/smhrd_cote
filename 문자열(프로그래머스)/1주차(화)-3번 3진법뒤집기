def solution(n):
    answer = ''
    
    while(n >= 1):
        namuji = n % 3
        n = n // 3
        answer = answer + str(namuji)    
        jjinanswer = int(answer,3)   #10진법으로 다시 표현하는 함수
        
    return jjinanswer
