person_str = input()  # "Mickey 1 10 1991"
person_lst = person_str.split()  # 문자열을 공백 기준으로 분리하여 리스트로 저장

# person_list ; ["M", "1", "10", "1991"]
year = [] 
month = [] 
day = [] 

while(True):
    year.append(int(person_list[3])
    month.append(int(person_list[2])
    day.append(int(person_list[1])
    
  # and 조건으로 다음 분기별 검사 시키기
    # 젤 어린사람 : year, month, day의 최댓값에 모두 충족하는 경우 
    # 젤 많은사람 : year, month, day의 최솟값에 모두 충족하는 경우 
               
           
